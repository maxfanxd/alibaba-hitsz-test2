modelscope==1.18.0
transformers==4.44.2
accelerate==0.34.2
datasets==2.20.0
peft==0.11.1
sentencepiece==0.2.0
streamlit==1.24.0
# 兼容性优先：使用 PyTorch DDP，无需 DeepSpeed
# 可选：4-bit 量化（仅 Linux，且需合适 CUDA），未安装将自动回退到 bf16
bitsandbytes>=0.42.0; platform_system=="Linux"